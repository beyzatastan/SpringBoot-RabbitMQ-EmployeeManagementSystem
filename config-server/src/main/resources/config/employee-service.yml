server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3307/ems_employee_db?useSSL=true&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ems_user
    password: EmsUser123!
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    prefer-ip-address: true
